import apiClient from './axiosConfig';

/**
 * A wrapper module for the configured Axios instance.
 * You can add common data fetching utilities here if needed, 
 * but for simplicity, we just export the configured client.
 */
export default apiClient;
